{
  "buildCommand": "pwd && ls -la && npx turbo run build --filter=api && find . -type f -name 'index.js' | grep -E '(dist|build)' | head -20",
  "outputDirectory": "apps/api/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
